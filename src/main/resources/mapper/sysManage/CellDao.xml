<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="uplord.uplordapi.sysManange.dao.CellDAO">
    <select id="findList" parameterType="CellVO" resultType="CellVO">
        SELECT
            C.CELL_CD
             , CELL_LEADER_UID
             , U.USER_NAME AS CELL_LEADER_NM
             , HIR_CELL_CD
             , CELL_NM
             , CELL_FEE_IDX
             , CELL_GROUP_PLACE
             , CELL_CONCEPT
             , CELL_NOTE
             , CELL_YEAR
             , C.USE_YN
        FROM CELL C
        LEFT JOIN USER U on C.CELL_LEADER_UID = U.USER_ID
     </select>
</mapper>