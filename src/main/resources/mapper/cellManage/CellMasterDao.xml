<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="uplord.uplordapi.cellManage.dao.CellMasterDAO">

    <select id="findAttendanceList" resultType="AttendanceDTO" parameterType="AttendanceDTO">
        SELECT *
        FROM CELL
    </select>

    <insert id="create" parameterType="AttendanceDTO">
        INSERT INTO CELL () VALUES ()
    </insert>

    <update id="update" parameterType="AttendanceDTO">
        UPDATE SET CELL WHERE
    </update>

</mapper>