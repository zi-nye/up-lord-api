<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="uplord.uplordapi.domain.attendance.dao.AttendanceDao">

    <select id="getAttendance">
    </select>

    <insert id="create" parameterType="RegisterAttendanceCommand">
        INSERT INTO ATTENDANCE (
            MEMBER_IDX
            , WRITE_DATE
            , MEMO
            , ATTENDANCE_YN
            , COMPLETE_YN
            , CREATED_UID
            , CREATED_AT
            , CREATED_IP
            , UPDATED_UID
            , UPDATED_AT
            , UPDATED_IP
        )
        VALUES (
              #{memberIdx}
            , #{writeDate}
            , #{memo}
            , #{attendanceYn}
            , #{completeYn}
            , #{createdUid}
            , #{createdAt}
            , #{createdIp}
            , #{updatedUid}
            , #{updatedAt}
            , #{updatedIp}
        )
    </insert>
</mapper>